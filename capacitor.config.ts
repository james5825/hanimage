import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yoel.imgprompthelper',
  appName: 'img-prompt-app',
  webDir: 'www',
};

export default config;
